package sunrise.model;

/**
 * Distinguishes the two staff roles supported by the system, as modelled
 * in the Use Case diagram (Receptionist and Admin are independent actors).
 */
public enum Role {
    RECEPTIONIST,
    ADMIN
}
